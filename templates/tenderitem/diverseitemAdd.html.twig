{% import 'components/form/fields_macros.html.twig' as fields %}

{{ fields.textField(
    'name',
    '',
    __('Name'),
    {
        'required': true
    }
) }}

{{ fields.numberField(
    'quantity',
    '',
    __('Quantity', 'tender'),
    {
        'required': true
    }
) }}


{{ fields.textareaField(
    'description',
    '',
    __('Description'),
    {
    enable_richtext: true,

    }
) }}

{{ fields.smallTitle( 
    __('Price', 'tender'),
)}}

{{ fields.numberField(
    'net_price',
    0,
    __('Net Price', 'tender'),
    {
        'step': ".01",
        'required': true
    }
) }}

{{ fields.numberField(
    'tax',
    19,
    __('Tax', 'tender'),
) }}

{{ fields.smallTitle( 
    __('Financial', 'tender'),
)}}

{{ fields.dropdownField(
    'GlpiPlugin\\Tender\\Account',
    'accountId',
    '',
    __('Account', 'tender'),
    {
        'to_add': accounts,
        'multiple': false,
        'required': true
    }
) }}

{{ fields.dropdownField(
    'GlpiPlugin\\Tender\\Costcenter',
    'costcenterIds',
    [],
    __('Costcenter', 'tender'),
    {
        'to_add': accounts,
        'multiple': true,
        'required': true
    }
) }}

{{ fields.dropdownField(
    'GlpiPlugin\\Tender\\Measure',
    'plugin_tender_measures_id',
    '',
    __('Measure', 'tender'),
    {
        'to_add': measures
    }
) }}

{# {{ fields.dropdownField(
    'GlpiPlugin\\Tender\\Financial',
    'financials',
    [],
    __('Financial', 'tender'),
    {
        'to_add': financials,
        'multiple': true
    }
) }} #}

{{ fields.smallTitle( 
    __('Location'),
)}}

{{ fields.dropdownField(
    'Location',
    'locations_id',
    item['default_locations_id'],
    __('Target Location', "tender"),
    {
        'required': true
    }
) }}

{{ fields.dropdownField(
    'Location',
    'delivery_locations_id',
    item['default_delivery_locations_id'],
    __('Delivery Location', 'tender'),
    {
        'required': true
    }
) }}


<script>
$(document).ready(function() {

    
});


</script>



