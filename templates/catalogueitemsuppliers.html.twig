{{ include('components/form/header.html.twig', {params: {target: '/plugins/tender/front/catalogueitemsupplier.form.php'}}) }}
{% import 'components/form/fields_macros.html.twig' as fields %}
   <div class="card-body d-flex flex-wrap">
      <div class="col-12 col-xxl-{{ item_has_pictures ? '9' : '12' }} flex-column">
         <div class="d-flex flex-row flex-wrap flex-xl-nowrap">
            <div class="row flex-row align-items-start flex-grow-1">
               <div class="row flex-row">
                  {{ fields.dropdownField(
                     'Supplier',
                     'suppliers_id',
                     '',
                     __('Suppliers'),
                     {'used': used}
                  ) }}
                  {{ fields.textField(
                     'suppliers_reference',
                     '',
                     __('Suppliers Reference', "tender"),
                  ) }}
                  {{ fields.textField(
                     'net_price',
                     0,
                     __('Net Price', "tender"),
                  ) }}
                  {{ fields.hiddenField(
                     'plugin_tender_catalogueitems_id',
                     item.fields['id']
                  ) }}
                           {{ fields.hiddenField(
                     'add',
                     'add'
                  ) }}
                  <input type="hidden" name="_glpi_csrf_token" value="{{ csrf_token() }}" />
               <div>
                  <button class="btn btn-primary me-2" type="submit" name="add" value="1">
                     <i class="fas fa-plus"></i>
                     <span>{{ _x('button', 'Add') }}</span>
                  </button>
               </div>
            </div>
         </div>
      </div>
   </div>
   <div class="col-12 col-xxl-{{ item_has_pictures ? '9' : '12' }} flex-column">
      <hr />
      {{ include('@tender/components/table.html.twig') }}
   </div>
</div>