{{ include('components/form/header.html.twig', {params: {target: '/plugins/tender/front/tenderitem.form.php'}}) }}
{% import 'components/form/fields_macros.html.twig' as fields %}

   <div class="card-body d-flex flex-wrap">
   {{ fields.smallTitle('Catalogue') }}
      <div class="col-12 col-xxl-{{ item_has_pictures ? '9' : '12' }} flex-column">
         <div class="d-flex flex-row flex-wrap flex-xl-nowrap">
            <div class="row flex-row align-items-start flex-grow-1">
               <div class="row flex-row">
                     {{ fields.numberField(
                        'quantity',
                        '',
                        __('Quantity'),
                     ) }}
                     {{ fields.hiddenField(
                        'tenders_id',
                        item.fields['id']
                     ) }}
                           {{ fields.hiddenField(
                     'add',
                     'add'
                  ) }}
                  <input type="hidden" name="_glpi_csrf_token" value="{{ csrf_token() }}" />
               <div>
                  <button class="btn btn-primary me-2" type="submit" name="add" value="1">
                     <i class="fas fa-plus"></i>
                     <span>{{ _x('button', 'Add') }}</span>
                  </button>
                  <a href="/plugins/tender/front/tenderitem.form.php" class="btn btn-primary me-2" type="submit" name="add" value="1">
                     <i class="fas fa-plus"></i>
                     <span>{{ _x('button', 'Add') }}</span>
                  </a>
               </div>
            </div>
         </div>
      </div>
   </div>
   {{ fields.smallTitle('Catalogue') }}
      <div class="col-12 col-xxl-{{ item_has_pictures ? '9' : '12' }} flex-column">
         <div class="d-flex flex-row flex-wrap flex-xl-nowrap">
            <div class="row flex-row align-items-start flex-grow-1">
               <div class="row flex-row">
                     {{ fields.dropdownItemTypes(
                     'itemtype',
                     item.fields['itemtype'],
                     __('Itemtype'),
                     {
                        types: itemtypes
                     }
                     ) }}
                     {{ fields.dropdownArrayField(
                        'catalogueitems_id',
                        '',
                        catalogueitems,
                        __('Catalogue Item'),
                        {'used': used}
                     ) }}
                     {{ fields.numberField(
                        'quantity',
                        '',
                        __('Quantity'),
                     ) }}
                     {{ fields.hiddenField(
                        'tenders_id',
                        item.fields['id']
                     ) }}
                           {{ fields.hiddenField(
                     'add',
                     'add'
                  ) }}
                  <input type="hidden" name="_glpi_csrf_token" value="{{ csrf_token() }}" />
               <div>
                  <button class="btn btn-primary me-2" type="submit" name="add" value="1">
                     <i class="fas fa-plus"></i>
                     <span>{{ _x('button', 'Add') }}</span>
                  </button>
                  <a href="/plugins/tender/front/tenderitem.form.php" class="btn btn-primary me-2" type="submit" name="add" value="1">
                     <i class="fas fa-plus"></i>
                     <span>{{ _x('button', 'Add') }}</span>
                  </a>
               </div>
            </div>
         </div>
      </div>
   </div>
   <div class="col-12 col-xxl-{{ item_has_pictures ? '9' : '12' }} flex-column">
      <hr />
      {{ include('components/datatable.html.twig') }}
   </div>
</div>